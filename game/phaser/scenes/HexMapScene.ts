import Phaser from 'phaser'; import { axialToPixel, HEX_SIZE } from '@/game/systems/HexGrid';
export class HexMapScene extends Phaser.Scene { constructor(private onHexClick:(q:number,r:number)=>void){super('HexMapScene');} create(){ for(let q=-6;q<=6;q++){ for(let r=-6;r<=6;r++){ if(Math.abs(q+r)>6) continue; const p=axialToPixel(q,r); const x=450+p.x,y=350+p.y; const hex=this.add.polygon(x,y,[0,-HEX_SIZE,36,-21,36,21,0,HEX_SIZE,-36,21,-36,-21],0x706960,0.18).setStrokeStyle(1,0xa08f78,0.4).setInteractive(); hex.on('pointerover',()=>hex.setFillStyle(0xb7a381,0.3)); hex.on('pointerout',()=>hex.setFillStyle(0x706960,0.18)); hex.on('pointerdown',()=>this.onHexClick(q,r)); } } } }
